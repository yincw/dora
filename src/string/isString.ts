/**
 *
 * @remarks isString
 *
 * @param `array` dataSource
 * @param `object` options
 * @returns `boolean`
 * @example
 *
 * @beta
 * @author yincw
 */

export const isString: any = (dataSource: any, options: any) => {
  if (!dataSource) throw new Error('isString 数据源参数不能为空。');
  // return ;
};

/* ******
 * 参考文献
 ****** */
// -
