/**
 * @public
 *
 * createOverload 入参接口类型
 *
 * @category interfaces
 *
 * @property useFirst 当 useFirst 为 true 时意味着，如果有多次相同的重载实现，则使用第一次的重载实现函数
 */
export interface OverloadOptionsInterface {
  useFirst: boolean;
}
