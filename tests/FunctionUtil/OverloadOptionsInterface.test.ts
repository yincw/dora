import { OverloadOptionsInterface } from '../../src/functionUtil/OverloadOptionsInterface';

// 测试套件
describe('functionUtil/OverloadOptionsInterface 函数', () => {
  // 测试块
  test('数据源参数不能为空', () => {
    // 预期
    // expect(() => {OverloadOptionsInterface()}).toThrow();
  });
});
