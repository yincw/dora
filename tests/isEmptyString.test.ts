import { isEmptyString } from '../src/string/isEmptyString';

// 测试套件
describe('string/isEmptyString 函数', () => {
    // 测试块
    test('数据源参数必填', () => {
        // 预期
        // expect(isEmptyString()).toThrow();
    });

});
