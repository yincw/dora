import { toPlainObject } from '../src/string/toPlainObject';

// 测试套件
describe('string/toPlainObject 函数', () => {
  // 测试块
  test('数据源参数不能为空', () => {
    // 预期
    // expect(() => {toPlainObject()}).toThrow();
  });
});
