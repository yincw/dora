import { isString } from '../src/string/isString';

// 测试套件
describe('string/isString 函数', () => {
    // 测试块
    test('数据源参数必填', () => {
        // 预期
        // expect(isString()).toThrow();
    });

});
