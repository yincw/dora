import { isNotEmpty } from '../../src/globalUtil/isNotEmpty';

// 测试套件
describe('globalUtil/isNotEmpty 函数', () => {
  // 测试块
  test('为 false 测试', () => {
    // 预期
    // const arr: any = [];
    // expect(isNotEmpty(arr)).toBe(false);
  });

  test('为 true 测试', () => {
    // 预期
    // const arr: any = [1];
    // expect(isNotEmpty(arr)).toBe(true);
  });
});
